version: '3.8'

services:
  backend_service:
    build:
      context: .
      args:
        PORT: 3000
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
